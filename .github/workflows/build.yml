#
# ============================================================================
# ImmortalWrt 固件自动构建 Workflow
# ============================================================================
# 作者: azxf
# 生成: Gemini AI
# 目标设备: 京东云 亚瑟和雅典娜 (IPQ60xx)
# 核心特性: APK 包管理器 + HomeProxy + Lucky + AdGuardHome + 流量监控
#
# 设计理念:
#   - 所有执行命令都提取到独立脚本文件中（scripts/目录）
#   - 脚本可在本地和 CI 环境中复用
#   - Workflow 只负责流程编排和环境管理
#   - 每次构建都拉取最新的源码和插件
# ============================================================================
#

name: 🚀 构建 ImmortalWrt IPQ60xx 固件

# ============================================================================
# 触发条件
# ============================================================================
# workflow_dispatch: 手动触发（在 Actions 页面点击 "Run workflow"）
# ============================================================================
on:
  workflow_dispatch:
    inputs:
      ssh:
        description: 'SSH 远程调试'  # SSH 连接到 Actions 虚拟机（用于调试）
        required: false
        default: 'false'

# ============================================================================
# 全局环境变量
# ============================================================================
# 这些环境变量会被脚本引用
# ============================================================================
env:
  REPO_URL: https://github.com/immortalwrt/immortalwrt  # ImmortalWrt 官方仓库
  REPO_BRANCH: master                                   # 主线分支
  FEEDS_CONF: feeds.conf.default                        # Feeds 配置文件
  CONFIG_FILE: seed.config                              # 本地配置文件名
  TZ: Asia/Shanghai                                     # 时区（用于日志时间戳）

# ============================================================================
# 权限配置
# ============================================================================
# 允许 workflow 创建 Release 和上传文件
# ============================================================================
permissions:
  contents: write  # 允许创建 Release、上传文件

# ============================================================================
# 构建任务
# ============================================================================
jobs:
  build:
    # ------------------------------------------------------------------------
    # 运行环境: Ubuntu 24.04 LTS（最新稳定版）
    # 使用 matrix strategy 以便将来扩展多系统支持
    # ------------------------------------------------------------------------
    strategy:
      matrix:
        os: [ubuntu-24.04]
    
    runs-on: ${{ matrix.os }}

    steps:
    # ------------------------------------------------------------------------
    # 步骤 1: 📥 检出仓库代码
    # ------------------------------------------------------------------------
    # 拉取当前仓库的所有文件（包括 seed.config 和 scripts/*.sh）
    # ------------------------------------------------------------------------
    - name: 📥 检出仓库代码
      uses: actions/checkout@v4

    # ------------------------------------------------------------------------
    # 步骤 2: 🔧 初始化编译环境
    # ------------------------------------------------------------------------
    # 执行脚本: scripts/01-init-environment.sh
    # 功能:
    #   - 使用 ImmortalWrt 官方初始化脚本
    #   - 自动安装所有编译依赖
    #   - 设置时区为 Asia/Shanghai
    #   - 创建工作目录 /workdir
    # ------------------------------------------------------------------------
    - name: 🔧 初始化编译环境
      env:
        DEBIAN_FRONTEND: noninteractive  # 非交互模式，避免 apt 弹窗
      run: |
        chmod +x scripts/*.sh
        bash scripts/01-init-environment.sh

    # ------------------------------------------------------------------------
    # 步骤 3: 📦 克隆最新源码
    # ------------------------------------------------------------------------
    # 执行脚本: scripts/02-clone-source.sh
    # 功能:
    #   - 克隆 ImmortalWrt 主线最新源码（浅克隆，节省时间）
    #   - 创建软链接到 $GITHUB_WORKSPACE/openwrt
    # 环境变量:
    #   - REPO_URL: ImmortalWrt 仓库地址
    #   - REPO_BRANCH: 分支名称（master）
    # 注意:
    #   - 使用 --depth=1 确保每次都拉取最新代码
    # ------------------------------------------------------------------------
    - name: 📦 克隆最新源码
      working-directory: /workdir
      run: bash $GITHUB_WORKSPACE/scripts/02-clone-source.sh

    # ------------------------------------------------------------------------
    # 步骤 3.5: 💾 恢复缓存
    # ------------------------------------------------------------------------
    # 使用 GitHub Actions 缓存机制
    # 缓存内容:
    #   - dl/ 目录：已下载的源码包（最重要，可节省大量下载时间）
    #   - .ccache/ 目录：编译缓存（加速重复编译）
    #   - staging_dir/ 目录：工具链缓存（跳过工具链编译）
    # 缓存键:
    #   - 基于配置文件哈希和构建编号
    #   - 滚动缓存，自动使用最近的缓存
    # ------------------------------------------------------------------------
    - name: 💾 恢复缓存
      uses: actions/cache@v4
      with:
        path: |
          /workdir/openwrt/dl
          /workdir/openwrt/.ccache
          /workdir/openwrt/staging_dir
        key: openwrt-cache-${{ hashFiles('seed.config') }}-${{ github.run_number }}
        restore-keys: |
          openwrt-cache-${{ hashFiles('seed.config') }}-
          openwrt-cache-

    # ------------------------------------------------------------------------
    # 步骤 3.6: 🔄 刷新缓存时间戳
    # ------------------------------------------------------------------------
    # 刷新 staging_dir 中的 stamp 文件
    # 作用:
    #   - 防止 make 认为缓存的文件过时
    #   - 确保缓存的工具链可以正常使用
    # ------------------------------------------------------------------------
    - name: 🔄 刷新缓存时间戳
      working-directory: /workdir
      run: |
        if [ -d "openwrt/staging_dir" ]; then
          echo "刷新 staging_dir 时间戳..."
          find openwrt/staging_dir -type d -name "stamp" -not -path "*target*" | while read -r dir; do
            find "$dir" -type f -exec touch {} +
          done
          echo "✅ 时间戳刷新完成"
        else
          echo "staging_dir 不存在，跳过刷新"
        fi

    # ------------------------------------------------------------------------
    # 步骤 4: 🔄 更新 Feeds 到最新版本
    # ------------------------------------------------------------------------
    # 执行脚本: scripts/03-update-feeds.sh
    # 功能:
    #   - 更新所有 feeds 源到最新版本（packages、luci、routing 等）
    #   - 安装所有 feeds 软件包到 staging_dir
    # 注意:
    #   - 每次都会拉取最新的 feeds 插件代码
    # ------------------------------------------------------------------------
    - name: 🔄 更新 Feeds
      run: bash scripts/03-update-feeds.sh

    # ------------------------------------------------------------------------
    # 步骤 4.5: 📦 更新 sing-box 到最新版本
    # ------------------------------------------------------------------------
    # 执行脚本: scripts/03.6-update-singbox.sh
    # 功能:
    #   - 自动获取 sing-box 最新 release 版本
    #   - 更新 feeds/packages/net/sing-box/Makefile
    #   - 跳过哈希验证（PKG_HASH:=skip）
    # ------------------------------------------------------------------------
    - name: 📦 更新 sing-box 版本
      run: bash scripts/03.6-update-singbox.sh

    # ------------------------------------------------------------------------
    # 步骤 5: 🧹 清理冲突插件
    # ------------------------------------------------------------------------
    # 执行脚本: scripts/04-clean-conflicts.sh
    # 功能:
    #   删除所有可能导致编译冲突的插件，包括:
    #   - VPN/代理类: SSR Plus、Bypass、VSSR、FcHomo 等
    #   - 多拨/负载均衡类: Mwan3、MultiAccount Dial 等
    #   - iStore/Docker/QoS 类: iStore、SQM、Natflow 等
    #   - DNS 解析类: SmartDNS、MosDNS、v2ray-geodata
    #   - 其他问题依赖: GNUnet、OnionShare CLI 等
    # ------------------------------------------------------------------------
    - name: 🧹 清理冲突插件
      run: bash scripts/04-clean-conflicts.sh

    # ------------------------------------------------------------------------
    # 步骤 5: 📦 克隆第三方插件
    # ------------------------------------------------------------------------
    # 执行脚本: scripts/03.5-clone-plugins.sh
    # 功能:
    #   从 GitHub 克隆最新的第三方插件到 package/custom 目录:
    #   - Lucky（综合工具箱）
    #   - HomeProxy（代理管理）
    #   - AdGuardHome（广告拦截）
    #   - EasyTier（组网工具）
    #   - Aurora 主题（现代化界面）
    # 优势:
    #   - 确保使用最新版本的插件
    #   - 不依赖 feeds 中可能过时的版本
    # 注意:
    #   - 在清理冲突后执行，避免第三方插件被误删
    # ------------------------------------------------------------------------
    - name: 📦 克隆第三方插件
      run: bash scripts/03.5-clone-plugins.sh

    # ------------------------------------------------------------------------
    # 步骤 5.6: 🔧 修复 sing-box Makefile
    # ------------------------------------------------------------------------
    # 执行脚本: scripts/03.6-fix-singbox.sh
    # 功能:
    #   - 修复 sing-box/Makefile 的循环依赖问题
    #   - 移除 full 版本，只保留 tiny 版本
    #   - 移除 PROVIDES/CONFLICTS 导致的Kconfig错误
    # ------------------------------------------------------------------------
    - name: 🔧 修复 sing-box Makefile
      run: bash scripts/03.6-fix-singbox.sh

    # ------------------------------------------------------------------------
    # 步骤 5.7: 🔧 修复 homeproxy Makefile
    # ------------------------------------------------------------------------
    # 执行脚本: scripts/03.7-fix-homeproxy.sh
    # 功能:
    #   - 修复 homeproxy/Makefile 的循环依赖问题
    #   - 从 LUCI_DEPENDS 移除 sing-box（避免自动 select）
    #   - 手动定义 Package 使用 DEPENDS
    # ------------------------------------------------------------------------
    - name: 🔧 修复 homeproxy Makefile
      run: bash scripts/03.7-fix-homeproxy.sh

    # ------------------------------------------------------------------------
    # 步骤 6: ⚙️ 加载配置文件
    # ------------------------------------------------------------------------
    # 执行脚本: scripts/05-load-config.sh
    # 功能:
    #   - 加载 seed.config 配置文件
    #   - 强制启用 APK 包管理器（新一代包管理）
    #   - 禁用旧的 OPKG 包管理器
    #   - 强制启用核心插件（双重保险）:
    #     * HomeProxy（代理管理）
    #     * Lucky（综合工具箱）
    #     * nlbwmon（流量监控）
    #     * wrtbwmon（带宽监控）
    #     * AdGuardHome（广告拦截）
    #   - 运行 make defconfig 自动补全依赖
    # 环境变量:
    #   - CONFIG_FILE: 配置文件名（seed.config）
    # ------------------------------------------------------------------------
    - name: ⚙️ 加载配置文件
      run: bash scripts/05-load-config.sh

    # ------------------------------------------------------------------------
    # 步骤 7: 📥 下载编译依赖
    # ------------------------------------------------------------------------
    # 执行脚本: scripts/06-download-libs.sh
    # 功能:
    #   - 使用 8 线程下载所有依赖包到 dl/ 目录
    #   - 检查下载失败的文件（小于 1KB）
    #   - 清理下载失败的残留文件
    # ------------------------------------------------------------------------
    - name: 📥 下载编译依赖
      run: bash scripts/06-download-libs.sh

    # ------------------------------------------------------------------------
    # 步骤 8: 🔨 编译固件
    # ------------------------------------------------------------------------
    # 执行脚本: scripts/07-compile.sh
    # 功能:
    #   - 尝试使用所有 CPU 核心并行编译（速度快）
    #   - 失败时自动切换到单核详细模式（V=s，便于排错）
    # 输出:
    #   - status=success（编译成功标记，供后续步骤判断）
    # ------------------------------------------------------------------------
    - name: 🔨 编译固件
      id: compile
      run: |
        bash scripts/07-compile.sh
        echo "status=success" >> $GITHUB_OUTPUT

    # ------------------------------------------------------------------------
    # 步骤 9: 📦 整理固件文件
    # ------------------------------------------------------------------------
    # 执行脚本: scripts/08-organize-files.sh
    # 功能:
    #   - 进入固件输出目录（bin/targets/*/*）
    #   - 删除 packages 目录（节省空间）
    #   - 输出固件文件列表
    # 输出:
    #   - FIRMWARE_DIR: 固件目录路径（用于上传）
    # 条件:
    #   - 仅在编译成功时执行
    # ------------------------------------------------------------------------
    - name: 📦 整理固件文件
      id: organize
      if: steps.compile.outputs.status == 'success'  # 仅在编译成功时执行
      run: |
        output=$(bash scripts/08-organize-files.sh)
        echo "$output"
        # 提取 FIRMWARE_DIR 环境变量
        FIRMWARE_DIR=$(echo "$output" | grep "FIRMWARE_DIR=" | cut -d'=' -f2)
        echo "FIRMWARE=$FIRMWARE_DIR" >> $GITHUB_ENV
        # 生成日期标识（YYYY-MM-DD格式）
        BUILD_DATE=$(TZ=Asia/Shanghai date '+%Y-%m-%d')
        echo "BUILD_DATE=$BUILD_DATE" >> $GITHUB_OUTPUT
        echo "status=success" >> $GITHUB_OUTPUT

    # ------------------------------------------------------------------------
    # 步骤 10: � 上传固件到 Artifacts（备份）
    # ------------------------------------------------------------------------
    # 使用 GitHub Actions 官方上传动作
    # 功能:
    #   - 将编译好的固件上传到 GitHub Artifacts（作为备份）
    #   - 保留时间: 90 天
    # 命名格式:
    #   - OpenWrt_固件_YYYY-MM-DD（按日期命名）
    # 条件:
    #   - 仅在固件整理成功时执行
    # ------------------------------------------------------------------------
    - name: � 上传固件到 Artifacts
      uses: actions/upload-artifact@v4
      if: steps.organize.outputs.status == 'success'
      with:
        name: OpenWrt_固件_${{ steps.organize.outputs.BUILD_DATE }}
        path: ${{ env.FIRMWARE }}

    # ------------------------------------------------------------------------
    # 步骤 11: 🚀 发布到 GitHub Releases
    # ------------------------------------------------------------------------
    # 创建 GitHub Release 并上传固件
    # 功能:
    #   - 创建新的 Release（按日期命名）
    #   - 上传所有固件文件到 Release
    #   - 永久保存，方便下载
    # Release 名称:
    #   - ImmortalWrt-IPQ60xx-YYYY-MM-DD
    # Tag 名称:
    #   - v-YYYY-MM-DD
    # 条件:
    #   - 仅在固件整理成功时执行
    # ------------------------------------------------------------------------
    - name: 🚀 发布到 GitHub Releases
      uses: softprops/action-gh-release@v1
      if: steps.organize.outputs.status == 'success'
      with:
        tag_name: op-${{ steps.organize.outputs.BUILD_DATE }}
        name: ImmortalWrt IPQ60xx 固件 ${{ steps.organize.outputs.BUILD_DATE }}
        body: |
          ## 📦 ImmortalWrt 固件发布
          
          ### 🎯 目标设备
          - 京东云 亚瑟（RE-SS-01）
          - 京东云 雅典娜（RE-CS-02）
          
          ### ✨ 核心特性
          - ✅ **APK 包管理器**（新一代包管理）
          - ✅ **HomeProxy**（代理管理）
          - ✅ **Lucky**（综合工具箱）
          - ✅ **AdGuardHome**（广告拦截）
          - ✅ **EasyTier**（组网工具）
          - ✅ **Aurora 主题**（现代化界面）
          - ✅ **流量监控**（nlbwmon + wrtbwmon）
          
          ### 📊 构建信息
          - 🕐 构建时间：${{ steps.organize.outputs.BUILD_DATE }}
          - 🏷️ 构建编号：#${{ github.run_number }}
          - 🌳 源码分支：${{ env.REPO_BRANCH }}
          - 🖥️ 运行环境：${{ matrix.os }}
          
          ### 📥 下载说明
          请根据您的设备型号下载对应的固件文件：
          - **雅典娜（RE-CS-02）**：选择包含 `re-cs-02` 的文件
          - **亚瑟（RE-SS-01）**：选择包含 `re-ss-01` 的文件
          
          ### ⚠️ 注意事项
          - 首次刷机请使用 `factory` 固件
          - 升级固件请使用 `sysupgrade` 固件
          - 刷机前请备份重要数据
        files: ${{ env.FIRMWARE }}/*
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    # ------------------------------------------------------------------------
    # 步骤 12: 📋 生成构建摘要
    # ------------------------------------------------------------------------
    # 输出构建摘要信息到 GitHub Actions Summary，方便查看
    # ------------------------------------------------------------------------
    - name: 📋 生成构建摘要
      if: steps.compile.outputs.status == 'success'
      run: |
        echo "## 🎉 固件构建成功！" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### 📊 构建信息" >> $GITHUB_STEP_SUMMARY
        echo "- 🕐 构建时间: $(TZ=Asia/Shanghai date '+%Y-%m-%d %H:%M:%S')" >> $GITHUB_STEP_SUMMARY
        echo "- 🏷️ 构建编号: #${{ github.run_number }}" >> $GITHUB_STEP_SUMMARY
        echo "- 🌳 源码分支: ${{ env.REPO_BRANCH }}" >> $GITHUB_STEP_SUMMARY
        echo "- 🖥️ 运行环境: ${{ matrix.os }}" >> $GITHUB_STEP_SUMMARY
        echo "- 💾 固件路径: \`${{ env.FIRMWARE }}\`" >> $GITHUB_STEP_SUMMARY
        echo "- 📦 固件名称: OpenWrt_固件_${{ steps.organize.outputs.BUILD_DATE }}" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### 📥 下载固件" >> $GITHUB_STEP_SUMMARY
        echo "在当前页面的 **Artifacts** 区域下载编译好的固件" >> $GITHUB_STEP_SUMMARY